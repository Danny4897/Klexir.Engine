# Klexir.Engine.Abstractions

Public contracts for the Klexir storage engine: `IPageStore`, `PageId`. Implementation (file I/O, buffer pool, B-Tree, WAL, transactions, query engine) is not part of this package.
